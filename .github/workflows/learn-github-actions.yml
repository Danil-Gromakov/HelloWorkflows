name: test-ci-python
on: [push]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls -l
      - run: pwd
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: ls -l
      - run: pwd
